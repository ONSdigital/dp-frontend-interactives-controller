---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-frontend-interactives-controller

run:
  path: dp-frontend-interactives-controller/ci/scripts/lint.sh